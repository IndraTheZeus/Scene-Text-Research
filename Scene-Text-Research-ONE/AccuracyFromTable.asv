function AccuracyFromTable(X)
    
    dev_value = 0.1;
    deviation = (X(:,1) - X(:,2))./X(:,1);
    
    y = deviation < dev_value;
    
    Unions = sum(X(:,3));
    Intersects = sum(X(:,4));
    
    IOU = Intersects/Unions;
    acc = sum(y)/numel(y);
    fprintf("ACCURACY WITH DEVIATION OF %d: %d percent    IOU = %d",dev_value,(acc*100),IOU);


end